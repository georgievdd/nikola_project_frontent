export const HOST = process.env.NEXT_PUBLIC_BACKEND_HOST
export const PORT = process.env.NEXT_PUBLIC_BACKEND_PORT
export const API_URL = `${HOST}:${PORT}/api/v1`
export const STATIC_URL = `${HOST}:${PORT}`
