version: '3'

services:
  frontend:
    env_file:
      - ../.env
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    networks:
      - nikola-docker-network
    volumes:
      - .:/app

networks:
  nikola-docker-network:
    external: true